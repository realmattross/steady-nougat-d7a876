[build]
  publish = "public"
  functions = "netlify/functions"
  command = "npm install"

[functions]
  node_bundler = "esbuild"

[functions."telegram"]
  timeout = 30

[functions."spotify-callback"]
  timeout = 10
